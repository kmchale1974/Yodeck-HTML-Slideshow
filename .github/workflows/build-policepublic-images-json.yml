name: Build PolicePublic images.json (Runtime Pi-safe)

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"
  push:
    paths:
      - "_Yodeck-HTML-Slideshow_PolicePublic/images/**"
      - "_Yodeck-HTML-Slideshow_PolicePublic_Runtime/images/.gitkeep"
      - "tools/gen_images_json.py"
      - "tools/build_runtime_media_policepublic.sh"
      - ".github/workflows/build-policepublic-images-json.yml"

permissions:
  contents: write

jobs:
  build-policepublic:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ffmpeg
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Build Runtime media (PolicePublic)
        run: |
          bash tools/build_runtime_media_policepublic.sh

      - name: Generate images-policepublic.json from Runtime images
        run: |
          python - <<'PY'
          from tools.gen_images_json import build_manifest
          build_manifest("_Yodeck-HTML-Slideshow_PolicePublic_Runtime/images", "images-policepublic.json")
          PY

      - name: Commit changes if any
        run: |
          git config user.name "gh-actions"
          git config user.email "actions@users.noreply.github.com"

          git add images-policepublic.json _Yodeck-HTML-Slideshow_PolicePublic_Runtime/images

          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi

          git commit -m "chore(policepublic): rebuild Pi-safe media + manifest [skip ci]"
          git push
